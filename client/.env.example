VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-goes-here

# Copy this file to `client/.env` and replace the values above for local development.
# Do NOT commit your real keys. `client/.env` is already added to the repo ignore list.
